max_retries = 1
retry_delay = 0.25
retry_backoff = 2.0
log_level = "INFO"
log_format = "json"
transaction_db_path = "rpacore.db"
screenshot_dir = "screenshots"
report_dir = "reports"
report_max_records = 1000

base_url = "https://acme-test.uipath.com"
credential_provider = "keyring"
headless = true
page_load_timeout_ms = 30000
action_timeout_ms = 10000

[queue]
db_path = "queue.db"
lease_timeout = 30
max_retries = 2
