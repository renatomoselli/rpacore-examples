[pytest]
testpaths = tests
python_files = test_*.py
markers =
    unit: deterministic tests without a browser
    integration: deterministic real-browser tests against localhost
    live: opt-in tests against the external ACME site
addopts = -ra --strict-markers
