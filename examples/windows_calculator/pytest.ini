[pytest]
pythonpath = .
testpaths = tests

markers =
    unit: Unit tests with mocked dependencies
    integration: Tests requiring real Calculator/desktop interaction
    live: Live desktop automation tests (Windows-only, opt-in)
    windows: Tests requiring Windows platform (pywinauto import dependency)

addopts = --strict-markers
